spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: ${DB_URL:jdbc:postgresql://localhost/person-service-db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:12345}

server:
  port: 8082
